# Copie ce fichier en ".env" (ne jamais le commiter) pour tester en local,
# ou passe ces valeurs via "docker run -e" / GitHub Secrets en production.
DB_HOST=IP_PRIVEE_DB
DB_USER=todo_user
DB_PASSWORD=change-moi
DB_NAME=todo_db
DB_PORT=5432
PORT=3000
